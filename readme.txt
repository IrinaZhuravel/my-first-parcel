🚀 Как запустить свой сборщик в новом проекте
1️⃣ Создай и склонируй новый репозиторий

ls  # Проверяем файлы в текущей директории
cd Documents/GitHub  # Для примера Переходим в папку, где будем хранить репозиторий
ls  # Проверяем, что внутри
git clone <ссылка_на_репозиторий>  # Клонируем новый репозиторий

2️⃣ Открыть проект в VS Code
Можно через code . в терминале (если VS Code установлен глобально) или открыть вручную.

3️⃣ Перенести файлы сборщика
Перенеси .github/workflows/deploy.yml и package.json из готового сборщика в новый проект.

4️⃣ Установить пакеты

npm install
5️⃣ Добавить исходные файлы
Добавь файлы проекта в папку src/.

6️⃣ Изменить пути в конфигурации
В файле package.json:

Измени homepage:
"homepage": "https://НИКНЕЙМ.github.io/НОВОЕ_ИМЯ_ПРОЕКТА/"

В scripts измени public-url:
"build": "parcel build src/*.html --public-url /НОВОЕ_ИМЯ_ПРОЕКТА/",
"deploy": "parcel build src/*.html --public-url /НОВОЕ_ИМЯ_ПРОЕКТА/ && gh-pages -d dist"
Можно и в других места в json. Удобно использовать Ctrl+H (заменить)

В deploy.yml:
Проверь, что путь в настройках деплоя совпадает с новым проектом.

7️⃣ Сделать коммит и пуш

git status  # Проверяем изменения
git add .  # Добавляем все файлы в коммит
git commit -m "Initial commit"  # Делаем коммит
git push  # Отправляем изменения в репозиторий

8️⃣ Проверить локально
Запустить локальный сервер:

npm start
(После запуска сайт будет доступен по адресу http://localhost:1234)

Остановить сервер: CTRL + C

Проверить сборку:
npm run build
(Если появилась папка dist, значит всё прошло успешно)

9️⃣ Настроить GitHub Actions (если нужно)
На GitHub:

Settings → Actions → General

Выбрать: Read and write permissions

Включить: Allow GitHub Actions to create and approve pull requests

🔟 Запустить деплой
npm run deploy
(Или просто пушнуть в main, если GitHub Actions настроен)

На GitHub:

Перейти в Settings → Pages
Выбрать gh-pages (если не выбралось автоматически)

✅ Готово! Теперь можно творить! 🎨